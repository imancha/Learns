$(function() {
	//	Datepicker
	$(".datepicker").datepicker({
		showInputs: false,
		format: 'yyyy-mm-dd'
	});
	//	Timepicker
	$(".timepicker").timepicker({
		showInputs: false,
		showMeridian: false
	});
});
